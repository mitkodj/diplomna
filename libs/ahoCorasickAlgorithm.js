var Q = require('Q');
var connection = require('./mysql_connection')();

function SPMA(query, SML) {
	var j = 0;
	for (; j < SML.length; j++) {

	}
}

function ACAlg(query, n, q0) {
	var state = q0,
		i = 0;
	for (; i < n; i++)
}

module.exports = {
	    SPMA: SPMA, 
	    login: login
};